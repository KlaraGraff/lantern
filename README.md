<div align="center">

<img src="assets/icon.png" width="112" alt="Lantern">

# Lantern

**把 AI 放进书里，而不是把书搬进聊天窗口。**

[![Release](https://img.shields.io/github/v/release/KlaraGraff/lantern?style=flat-square&color=1f6feb&label=%E6%9C%80%E6%96%B0%E7%89%88%E6%9C%AC)](https://github.com/KlaraGraff/lantern/releases)
[![Platform](https://img.shields.io/badge/%E5%B9%B3%E5%8F%B0-macOS%20%C2%B7%20Windows-555?style=flat-square)](#支持的平台)
[![License](https://img.shields.io/badge/License-MIT-555?style=flat-square)](LICENSE)

[简体中文](README.md) · [English](README.en.md) · [下载](https://github.com/KlaraGraff/lantern/releases)

</div>

<!-- 截图占位：阅读器全景（正文 + 查词卡 + AI 侧栏）。拍摄要求见 docs/guide/screenshots.md
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![Lantern 阅读界面](assets/screenshots/hero.png)
-->

---

## 你早就在用 AI 读书了，只是方式很别扭

读到一段看不懂的，复制，切窗口，粘进聊天框，问它。

它给了你一段回答——但它不知道你在读哪本书，不知道你读到第几章，不知道这句话前后写了什么，也不知道你的英语到什么程度，所以它默认按母语者的口径回答你。看完了，切回阅读器，刚才读到哪儿还得再找一遍。而它给的那个答案是悬空的：你想回去核对原文，只能自己翻。

**Lantern 是反过来做的。** 它是一款 macOS 和 Windows 上的桌面阅读器，本地优先，用你自己的 AI 服务。它不把书一段一段搬进聊天窗口，而是**把 AI 放进书里**——那些你懒得每次交代的事，它一开始就知道。

具体是四件事：

1. **它拿得到上下文**——你的英语水平、读到哪、在读哪一本哪一章、选中处前后是什么，都随每一次请求过去。
2. **它说的每一句都能点回原文**——给的不是一个孤零零的答案，而是能带你回到书里那一句的答案。
3. **它说什么、怎么说，由你写**——预设模块不合用，自己写提示词造一个。
4. **它甚至可以不是它**——通过 MCP 把书库和生词本交给 Claude Code、Codex，换成你自己惯用的 AI 来读。

---

## 一 · 它知道的，比一个聊天窗口多得多

一个通用聊天窗口拿到的只有你粘进去的那几行字。Lantern 每次请求带过去的，还有下面这些——**不需要你交代，也不需要你重复交代**：

| 它知道 | 于是 |
| --- | --- |
| 你的英语水平 | 解释的用词和句子复杂度跟着变，而不是永远按母语者的口径来 |
| 你读到哪 | 默认不拿你还没读到的地方来回答你 |
| 你在读哪一本、哪一章 | 认得出书里的人名、地名和前情，不用你每次说「这是《XX》里的」 |
| 选中处前后是什么 | 先定住这个词在这里的意思，再展开——而不是罗列一堆词典义项让你自己挑 |
| 整本书 | 答案是从书里检索出来的，不是靠模型对这本书的记忆 |

### 它知道你的水平

大多数阅读工具的「英语解释」是一个开关：打开，就给你一段接近母语者的英文。对 B1 的读者来说，这段解释本身就是新的障碍。

Lantern 把英语水平当作**每一次请求的条件**，而不是界面上的一个偏好。你可以在个人资料里直接填 CEFR 等级（A1–C2），也可以填雅思、托福、托业、剑桥英语、DET 或四六级成绩，由应用换算成学习等级。

| 你的等级 | AI 怎么解释 |
| --- | --- |
| **A1–A2** | 以准确的中文义为基础，配一句符合当前水平的简明英文。不会「用更难的英文解释简单英文」。 |
| **B1** | 以英文解释为主；抽象的、容易误解的部分保留必要的中文辅助。 |
| **B2–C2** | 提供更自然、更深入的英语解释，需要时再展示目标语言翻译。 |

不过「照顾水平」很容易滑向另一个毛病：为了简单而牺牲准确，把精确的词换成含糊的词。所以这里还有一条硬规矩——**解释里如果非用一个超出你水平的词不可，那就保留它，并且当场就地加注**，用几个更简单的英文词或一个中文括注说清楚。**不许换成更含糊的词，也不许扔在那儿让你另外去查。**

选「英语优先」因此不等于接受看不懂的母语级解释——解释本身也是**可理解的输入**。你可以从「先看懂」开始，慢慢过渡到「用英语理解英语」，而不必被一段读不懂的英文再打断一次。

<!-- 截图占位：学习等级设置界面，以及同一个词在 A2 与 C1 下的两张查词卡对比
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![按英语水平调整的解释](assets/screenshots/level.png)
-->

### 它知道你读到哪

**它默认不会拿你还没读到的地方来回答你。** 一旦提前知道后面发生了什么，你的思路会不自觉地朝那个方向收拢——你本来会自己琢磨出来的东西，就没有机会出现了。

所以「阅读思考保护」把回答限制在你已经读过的范围内，答案上标明「已按你的阅读进度回答（前 X%）」。等你确实想要完整的答案，点一下「结合全书重新回答」。这个开关可以按书单独设。

要它看多大范围也由你定：自动、只看选区、只看本章、或者全书检索。

这条边界也延伸到了人物和术语。读到一个一时想不起的人名，或者反复出现却还没说透的意象，直接从正文打开一张轻量卡片：它会整理身份、别名、已知关系和此前出场，也会解释术语在这本书里的含义。**卡片默认只看到你当前读到的位置**，不会因为查一个名字就顺手告诉你后面发生了什么。

确实需要整书视角时，Lantern 会先明确告诉你这会包含未读情节，再让你确认。整书范围只对这一次查看生效；关掉卡片，下次仍然从无剧透的范围开始。

<!-- 截图占位：AI 侧栏的上下文控件——回答范围选择器、阅读思考保护开关，以及答案下方的进度提示与「当前阅读范围原文」
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![它知道你读到哪](assets/screenshots/context.png)
-->

---

## 二 · 它说的每一句，都能点回原文

开始读一章之前，你可以先让它把这一章里你可能不认识的词和短语列出来。它给你的是一份长清单——而清单里的每一条后面都跟着一个角标，**点一下，书就翻到那个词所在的那一句**。

读到一半想问剧情也一样。它不会甩给你一个答案就完了，而是把依据一起给出来，每条依据同样是可以点的。

这不是「回答下面附一个参考列表」。做这件事最好的产品是 NotebookLM，但它的引用只能跳到侧栏里的一段文本——**Lantern 本身就是阅读器，所以引用跳回的是书里真正的那一页、那一句**，落点还会闪一下，告诉你眼睛该看哪。

具体怎么工作：

- **引用是内联的。** 回答正文里直接带角标，回答末尾再列成一排，两处都能点。一次生词清单引用三十几处也不会乱。
- **定位是按原文找的，不是按页码猜的。** EPUB 拿原文片段在对应章节里精确搜索，找不到就换一个备用片段，再不行退到章节开头；PDF 跳到对应页；纯文本按字符偏移直接定位。
- **答案是从书里检索出来的**，不是靠模型记忆。全书建了全文索引，配了 embedding 服务的话还会叠一层语义检索——所以冷门书、新书和你自己的文档，它一样答得准。
- **而且它受上一节那些约束。** 引用不会来自你还没读到的地方，检索范围也听你的——所以「读之前列生词」这件事才成立：它扫的是你即将读的这一章，不是整本书。

### 隔了几天回来，存下来的东西还挂在书上

聊天软件里的历史记录是一条越堆越长的流水：你三天前问的那个句子，混在报销流程和菜谱中间，翻回去找不到，找到了也想不起来当时读到哪。生词本也是同一个毛病——词孤零零躺在列表里，和它出现的那一句断了联系，剩下的只能硬背。

Lantern 里存下来的东西都还连着书。生词、笔记、对话各自按书归类，可以搜，能看到是几天前的；每一条旁边都挂着「**在阅读器中打开**」，点下去落回的不是书的开头，是当时那一句。生词卡上还留着它出现的那段原文，和它**在那一句里**的意思——不是词典里的第一个义项。

笔记甚至不必离开正文去找。页边笔记轨把笔记卡放在它所引用的段落旁边，正文会为它让出空间，不会被浮层盖住；点一条笔记，原文与笔记仍然保持在同一个视野里。

复习时，原句也不会被丢掉。Lantern 把生词从原句里挖空，第一层提示是读音，第二层是你已经保存的中文句意，最后才揭开答案——不拿字母数和首字母替你猜。需要把一本书带走时，高亮、备注和生词可以在本地整理成 Markdown、CSV 或 Anki CSV，再交给你自己的笔记和复习工具。

对话更进一步：书打开的同时，**那段对话也跟着回到侧栏**，你接着上次的话头继续问，里面那些角标依然是活的。

所以不只是「它说的每一句能点回原文」，是**你在 Lantern 里攒下的一切，都长在书上**。

<!-- 截图占位：AI 回答里带角标的生词清单，以及点击角标后跳转到的原文位置
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![可点击的引用](assets/screenshots/citations.png)
-->

---

## 三 · 预设模块只是起点，工具应该由你来造

一张查词卡上该出现什么？语境含义、词性、常见义项、搭配、词根词缀、语法角色、近义词、用法、记忆提示、原句出处——Lantern 内置了这些模块，单词卡 11 个、短语卡 8 个、段落卡 9 个，每一个都能单独开关、排序、设默认展开状态和内容密度。

**但预设不该限定你的学习方法。**

如果现有模块不够用，你可以自己造一个 AI 模块：

- 给模块起名字，写**完全属于你自己的提示词**（最长 2000 字）。
- 让它基于当前选区、上下文和书籍信息生成内容。
- 把它加进单词、短语或段落卡片，和内置模块一起显示、隐藏、排序、设展开。
- 在设置里即时预览卡片结构；确认要看真实效果时，再调用一次真正的 AI。
- 每种卡片最多 8 个自定义模块，另外还能建 6 个自定义选区操作，绑快捷键或双击触发。

比如你可以做出这些：

- 面向雅思或考研的**长难句拆解**模块；
- 用于文学作品的**修辞、叙事视角与语气**模块；
- 用于专业书籍的**术语、前置知识与应用场景**模块；
- 用于写作积累的**可复用表达与改写建议**模块；
- 只保留最少信息、不打断沉浸阅读的**极简查词**模块。

<!-- 截图占位：自定义模块编辑器（提示词输入）+ 卡片设计设置的模块排序
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![自定义 AI 模块](assets/screenshots/modules.png)
-->

---

## 四 · 换成你自己的 AI，也行

前面三节讲的都是「Lantern 把上下文给了它内置的 AI」。但那些上下文凭什么只有它自己能用？

Lantern 内置了一个 **MCP 服务器**。打开它，Claude Code、Codex 这些 AI 客户端就能直接读你的阅读数据——**你不用复制粘贴任何东西**：

| 它们能读到 | 具体是什么 |
| --- | --- |
| 你的书库 | 书目、合集、元数据、书的正文与分块索引状态 |
| 你的语言档案 | CEFR 等级和你填的雅思 / 托福等成绩——**和内置 AI 用的是同一份** |
| 你的学习记录 | 生词本、掌握状态、统计、查询历史、词形标记 |
| 你留下的东西 | 高亮、书签、笔记、书内对话历史、全书与章节摘要 |

一共 29 个工具：12 个只读上下文查询、一个「在阅读器中打开」动作、16 个写入工具。默认只读；**写权限（导入书、删书、建合集这些）是一个单独的开关，默认关着**——把数据交出去和把控制权交出去是两回事，不该捆在一起。删数据或覆盖式导入还会针对那一次操作单独确认；MCP 不会替你的 AI 再去调用模型。

于是你可以在终端里直接问：「按我的英语水平，从我书库里挑三本能读下去的」「把我这个月标为『学习中』的词整理成一份带例句的清单」。它读的是你真实的阅读记录，不是你临时描述给它听的版本。

<!-- 截图占位：MCP 设置页与 Claude Code 终端并排，终端里正在按真实书库回答
     图拍好放进 assets/screenshots/ 后，删掉本注释的首尾两行即可显示。
![把书库交给 Claude Code](assets/screenshots/mcp.png)
-->

---

## 五 · 每一处都能改，而默认值已经替你想过

前面几节里的解释口径、检索范围、卡片模块，其实都是同一件事的不同侧面：**Lantern 里几乎没有写死的地方。** 颜色、样式、模块、快捷键、朗读音源、卡片上显示什么、选中文字后弹出什么——能调的都能调。

但「什么都能调」如果意味着「你得先调完才能用」，那只是把活推给了你。所以每一项都有默认值，而且这些默认值是认真想过的：装上就能直接读，只在你觉得哪里不顺手的时候，才需要去动它。

正文标记是这两点体现得最集中的地方。

### 一本书可以例外，但不必永远例外

排版设置先有一套全局默认，一本书也可以保留自己的例外。右侧同一个设置面板会明确告诉你当前是「本书单独设置」，并允许你恢复全局、把这本书的效果提升成新的全局默认，或者再挑几本书一起应用。它不会为了管理作用域再叠一个弹窗，也不会在同步后留下已经恢复过的旧设置。

可调的不只是字号和行距。EPUB 可以开启两端对齐和按语言自动断词，段距有四档，首行缩进会按语言和段落类型适配。默认仍尊重出版社原本的排版；PDF 这类固定版式也不会显示它做不到的选项。

### 标记：两套样式，六个维度

**手动标记**和**查词自动标记**是两套完全独立的样式，各有颜色、透明度、高亮 / 下划线 / 加粗、字体六个维度。想让两者长得一样，有一个「跟随手动标记」的开关。

默认值不是随手定的：手动标记是暖黄实底——那是你自己标的，就该显眼；自动标记是很淡的褐色加下划线——那是查词留下的痕迹，应该能一眼读过去。两者靠**形状**而不只是颜色区分，所以永远不会认错。

- **加粗和换字体默认关闭。** 因为它们会改变文字宽度：在分页模式下，一个标记出现就会让这一页重新排版，换行位置和页边界跟着变。颜色、背景、下划线不会移动任何文字。想要就打开，但你得先知道代价。
- **应用自己也在正文上画东西**：朗读到的位置是一层冷蓝的洗色，生词 / 学习中 / 已掌握是三档下划线——从暖橙到青再到灰色虚线，是一条「注意力逐步撤走」的渐变，不是四种互不相干的颜色。
- **所以选色时它会拦你一下**：如果你挑的颜色和上面这些太接近，设置里会当场提示「正文里可能分不清」，并指出撞的是哪一个。这个判断是把两个颜色**在真实纸底上混合之后**再算色距，不是比 HEX——一个洗色和一条下划线在色板上离得远，铺到纸上可能就分不开了。预设色也是按「离系统标记最远」挑的，但预设只降低概率，色号输入框和取色盘照样接受任何颜色。

### 词形标记：标记的是一个词，不是一串字符

你在书里查了 `run`。这本书后面出现的是 `ran`、`running`、`runs`——按字符匹配，它们和 `run` 毫无关系，一个都不会被标记。但对读者来说，它们本来就是同一个词。

所以「标记单词时」有三档，不是两档：

| 选项 | 行为 |
| --- | --- |
| 仅标记当前位置 | 只标这一处 |
| 标记本书中的相同单词 | 标全书所有拼写完全一样的 |
| **同单词各词形** | 标全书这个词的**所有词形** |

**词形不需要你手动录入。** 词形管理器里每个已标记的单词旁边有一个按钮，点一下就让 AI 生成它的词形；也可以一键批量补全所有还没有词形的词（10 个一批、5 路并发、失败自动重试一次、随时可取消，带进度）。

生成完还能改——你觉得 AI 漏了或者给多了，直接在输入框里编辑。手动改过的会被记成你自己的版本，不会被后续的批量生成覆盖。

### 别处也是一样的

- **朗读音源**默认「自动」：查一个词用词典的真人录音，读一整段用神经语音，两者都失败退回系统语音。但四种音源你都可以直接指定。开启连续朗读后，顶部播放器可以切上一句 / 下一句、暂停、停止和调语速，并自动跨过段落、翻页和章节；不需要时可以收成一枚小胶囊。
- **生词辅助**可以把已保存单词的短释义直接放回正文，选词上小字或页边释义，再决定显示密度。阅读页的快捷开关和主设置控制的是同一个全局状态，开启本身不会再调用 AI。
- **增强字体显示**是一个设备本地的可选字体包：需要时下载并启用，不需要时可以停用但保留文件，也可以明确移除来释放空间。普通界面仍优先使用系统字体，不为了字体把基础安装包撑大。
- **书籍来源站点**是一份可以随便增删的清单，不是固定名单。
- **三击选中什么**、快捷键怎么绑、要不要在选区菜单里显示快捷键——都是设置项。
- **学习卡片的内容密度**有一个说明弹窗，逐个模块告诉你「简洁 / 标准 / 详细」各自会多出什么，让你知道自己在选什么。

---

## 六 · 统计不是监督，是把读过的时间还给你

很多阅读统计最后都变成了另一种打卡：连续多少天、今天还差几分钟、谁读得更多。Lantern 记录阅读，不是为了催你完成目标，而是为了让你隔一段时间回头时，能看见时间真正留给了哪些书。

同一个入口里有两个视角。**阅读历程**默认按书回答「读了什么、读到哪里、花了多久」；**阅读日历**按日期找回某一天打开过的书和有效会话。两边共用范围、书籍筛选和总览数字，不设排行、连续天数或惩罚。连续 5 分钟没有阅读活动会暂停计时，少于 30 秒的片段会被丢掉——打开书找一句话，不会被算成一次阅读。

那些更像人写的回顾，例如你常在什么时间读书、哪一本陪你最久，可以交给 AI 来组织语言。但**事实先由 Lantern 在本地确定性计算，AI 只负责叙述，不能重新计算，更不能补出不存在的数据。** 回顾必须由你主动生成或刷新，旧结果会缓存在本机。

首次生成前，界面会把当前服务商、要发送的数据范围和可能的额度 / 费用说清楚。默认发送的是总时长、会话数、书籍数、阅读天数等可复算的结构化事实，**不发送正文、高亮原文、笔记正文、精确会话时间线或未读内容**。

---

## 三分钟上手

1. **下载安装** —— 从 [Releases](https://github.com/KlaraGraff/lantern/releases) 取对应平台的安装包。macOS 首次打开时 Gatekeeper 会要求确认（原因见[下载](#下载)）。
2. **填一个 AI 服务** —— 打开「设置 → AI 服务」，填 OpenAI 兼容 API、Anthropic、Ollama 或自建网关，测试连接。密钥只存在这台设备上。
3. **拖一本书进来** —— 把 EPUB / PDF / TXT 拖进书库，打开它，**双击任意一个单词**。

想让它更贴合自己：去「设置 → 个人资料」填英语等级，去「设置 → 卡片设计」调你想看的模块。

---

## 完整功能

<table>
<tr><td width="180"><b>AI 理解</b></td><td>

语境查词 · 短语释义 · 段落解读 · 整段翻译 · 书内持续对话（把单词、原句、已有释义和阅读位置一并带进对话）· **每条回答带可点击的原文引用，点一下跳回书里那一句** · 全文索引检索，配了 embedding 再叠一层语义检索 · 回答范围可选自动 / 选区 / 本章 / 全书 · **阅读思考保护**：默认只用你已读过的部分回答，把后面的推理空间留给你自己，可一键改为结合全书 · 人物 / 术语轻卡整理身份、关系、书内含义和此前出场，同样默认截至当前位置 · 明确展示本次带进对话的原文，可随时移除 · **对话按书归档、可搜索**，一键回到阅读器并把当时那段对话一起带回来

</td></tr>
<tr><td><b>学习闭环</b></td><td>

生词本 · 查询历史 · 「新词 / 学习中 / 已掌握」学习状态 · FSRS 间隔重复复习 · 原句挖空复习，按读音 → 已保存中文句意 → 答案逐层提示 · 笔记中心与阅读页页边笔记轨 · CSV / JSON 词汇导入导出 · 当前书高亮、生词和备注本地导出为 Markdown / CSV / Anki CSV · 一键回到原文位置

</td></tr>
<tr><td><b>正文标记</b></td><td>

查词后自动标记 · 三档匹配范围：当前位置 / 全书同词 / **同词各词形**（词形由 AI 一键生成，可手动改）· 已保存生词可显示词上小字或页边释义，无需再次调用 AI · 手动高亮 · 手动与自动两套独立样式，各含颜色、透明度、高亮 / 下划线 / 加粗与字体 · 选色撞上系统标记时当场警告 · 不改动原书内容

</td></tr>
<tr><td><b>阅读体验</b></td><td>

暖色纸感主题与自定义背景色 · 导入自定义字体与设备本地增强字体包 · 字号、行距、页边距、两端对齐、语言断词、段距、首行缩进 · 全局默认与本书单独设置，可恢复或提升为全局 · 滚动与分页切换 · 书签 · 目录侧栏 · 阅读进度 · 页边笔记轨 · 多窗口同时读多本 · 合集文件夹管理书库

</td></tr>
<tr><td><b>朗读</b></td><td>

四种音源：词典真人录音 / 系统语音 / Edge 神经语音 / 自定义 OpenAI 兼容 TTS · 自动按内容长短和语言选音源 · 顶部连续朗读播放器 · 上一句 / 下一句 / 暂停 / 停止 / 语速 · 自动跨段、翻页和章节 · 逐句跟随高亮 · 暂停后从停下的地方继续

</td></tr>
<tr><td><b>阅读回顾</b></td><td>

阅读历程与阅读日历双视图 · 按范围和书籍筛选 · 5 分钟闲置暂停、30 秒以下片段丢弃 · 本地可复算的时长、会话、书籍和阅读天数 · 可选 AI 回顾只负责叙述结构化事实，主动生成、发送范围可见、结果本地缓存

</td></tr>
<tr><td><b>AI 服务</b></td><td>

OpenAI 兼容 API · Anthropic · Ollama · 可选 OpenAI OAuth · 可加多个服务并设优先级 · 每个服务可存多个 Key，输出开始前按优先级自动试可用的 Key 和服务 · 连接测试与模型发现

</td></tr>
<tr><td><b>集成与工具</b></td><td>

MCP 服务器——把书库、生词本和笔记开放给 Claude Code、Codex 等 AI 客户端，读写权限可关 · 扫描版 PDF 的 OCR · 可编辑的书籍来源站点列表 · 书籍元数据编辑

</td></tr>
</table>

---

## 支持的平台

| 平台 | 支持范围 |
| --- | --- |
| **macOS** | macOS 12 Monterey 或更高，**仅 Apple Silicon（M 系列）**。主要平台，功能最全。 |
| **Windows** | 提供 Windows 11 x64 安装包。可本地阅读并使用全部 AI 功能，**不含 iCloud 文件夹同步**。 |
| Intel Mac | 当前不提供安装包。 |
| Linux | 当前不提供发行版本。 |
| iOS | 开发中，尚未发布。进度见 [路线图](docs/roadmap/mobile-ios.md)。 |

---

## 支持的格式

| 格式 | 导入方式 | 阅读控制 | 选择与手动高亮 | 自动词汇标记 |
| --- | --- | --- | --- | --- |
| **EPUB** | 原生阅读 | 字体、行距、页边距、滚动 / 分页 | ✅ | ✅ |
| **TXT · Markdown · HTML** | 保留原文件，转换为稳定的内部 EPUB | 同 EPUB | ✅ | ✅ |
| **PDF** | 原生阅读 | 主题、缩放、单页 / 双页、滚动 / 分页 | 有可用文本层时 ✅ | ❌ |
| **MOBI · AZW · AZW3 · FB2 · FBZ** | 通过 Foliate 原生解析器 | 渲染器支持时可用流式控制 | ❌ | ❌ |
| **CBZ** | 原生阅读 | 仅主题 | ❌ | ❌ |

以上描述的是当前的本地导入与阅读器集成能力。不支持 DRM，也不保证完美渲染每一种出版商特定的文件变体。

---

## 你的数据在哪

- **书库数据本地优先。** 书、阅读进度、生词本、笔记和阅读统计都存在这台设备上。
- **API Key 与 OAuth 令牌只保存在本机的凭据数据库里**，不会返回给界面层，也不参与同步。
- **需要多设备同步时**，在设置里选一个你自己 iCloud Drive 里的文件夹，每台 Mac 选同一个。应用把事件日志、书籍和封面放在那里。当前版本不使用原版 Quill 的 iCloud 容器，也不宣称与原版 Quill 的 iOS 应用或其私有 iCloud 数据兼容。
- **AI 请求默认只发送完成当前任务所需的上下文**，不会自动上传整本书。AI 阅读回顾只有在你主动生成时才发送界面列出的结构化事实，默认不包含正文、笔记或高亮原文。

---

## 下载

安装包和发行说明发布在 [Releases](https://github.com/KlaraGraff/lantern/releases)。

macOS 构建目前使用临时签名（ad-hoc），因此 Gatekeeper 会在首次运行时要求确认。签名与公证计划见 [macOS 分发](docs/guide/macos-distribution.md)。在这个分支有自己的签名发行渠道之前，自动更新保持关闭。

---

## 开发

要求：Node.js 22、npm、Rust，以及目标平台的 Tauri 前置依赖。阅读器引擎（foliate-js）源码已随仓库提交。

```bash
git clone https://github.com/KlaraGraff/lantern.git
cd lantern
npm ci
npm run tauri dev
```

常用静态检查：

```bash
npm exec tsc --noEmit
npm run lint
cd src-tauri && cargo check
```

技术栈：Tauri 2 + Rust + SQLite（后端），React 19 + TypeScript + Tailwind 4（前端），foliate-js（EPUB 渲染）。仓库协作约定见 [AGENTS.md](AGENTS.md)。

---

## 致谢与许可证

Lantern 基于 yicheng47 开发的 [Quill](https://github.com/yicheng47/quill)，是独立维护的个人版本，并非原项目的官方发行版。原版 Quill 的版权仍归其作者所有；本仓库保留原始 [MIT License](LICENSE)，包括其中的版权声明。
</content>
