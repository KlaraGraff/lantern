# 截图拍摄指南

README 和后续推广共用同一批截图。这份文档是拍摄规范和镜头清单，文末附一段可直接交给另一个 AI 助手（或人）执行的提示词。

**重要前提：这些是真实软件界面的截图，必须由真正运行起来的 Lantern 产出。** 不要用图像模型生成界面——生成出来的按钮、文字和布局一眼就能看出是假的，会直接损害产品可信度。图像模型只用来做推广物料的背景板和封面，不用来做界面。

## 现状

`assets/home.png` 和 `assets/reader.png` 是旧图：侧栏还写着 "Quill"，用的是旧的紫色配色，和当前版本（v2.6.0）对不上。这批新图拍好后，README 的占位路径就能生效，旧图可以删掉。

## 环境准备

1. 在 `~/vibecoding/Lantern` 里跑 `npm run tauri dev`（或直接用已安装的 Release 版本，更接近用户看到的样子）。
2. **书库准备 12–15 本书**，让书架看起来是真在用的，不是空的。
   - **优先用公版书**（Project Gutenberg 上的 *Pride and Prejudice*、*Frankenstein*、*Moby-Dick*、*Walden*、*Alice's Adventures in Wonderland* 等）。推广图会被大量转发，用在版书的封面有版权风险。
   - 混一点中文书和不同类型的封面，让书架有视觉层次。
   - 建 3–5 个合集文件夹（Fiction、History、Sci-Fi……），并且让每个都有书。
3. **设置项**：
   - 界面语言：中文和英文各拍一套（英文那套给 `README.en.md` 用）。
   - 阅读主题：暖色纸感（默认主题），不要深色——深色截图在社交平台的白底信息流里会糊成一块。
   - 个人资料里的英语等级：拍 Shot 2 时需要在 A2 和 C1 之间切换。
4. **必须清理的**：
   - 任何真实 API Key、邮箱、账号名。设置页截图前把 Key 字段留空或换成明显的假值（`sk-••••••••`）。
   - 桌面通知、其他应用的窗口、系统托盘里的私人信息。

## 拍摄规范

| 项目 | 要求 |
| --- | --- |
| 窗口尺寸 | 1440 × 960（应用默认值，见 `src-tauri/tauri.conf.json`） |
| 分辨率 | Retina 屏拍摄，实际输出 2880 × 1920 |
| 格式 | PNG |
| 截图方式 | macOS 上 `⌘⇧4` 然后按空格选窗口，会自动带上窗口圆角和阴影 |
| 背景 | 纯色或极浅的渐变，不要拍到桌面壁纸 |
| 文件大小 | 单张压到 500 KB 以内（用 ImageOptim 或 `pngquant`），README 里 5 张图加起来别超过 2 MB |

## 镜头清单

按顺序拍，文件名严格对应，直接放进 `assets/screenshots/`。

### 1. `hero.png` —— 首图，最重要的一张

**内容**：阅读器全景，三样东西同时在画面里：

- 左侧：正文，暖色纸底，一段读起来舒服的英文段落（不要停在章节标题或空白页）。
- 中间：一张查词卡浮在正文上，词选得要有代表性——一个语境里含义和字面义不同的词最好。卡片上要能看到语境含义、简短中文义、英文定义。
- 右侧：AI 对话侧栏展开，里面有一轮已完成的对话，能看出它引用了原文的哪一句。

**要点**：这张图要在 0.5 秒内说清「这是个什么软件」。三个区域都不能空，但也别让侧栏挤掉正文——正文仍然应该是画面里面积最大的部分。

### 2. `level.png` —— 分级解释

**内容**：同一个单词在两个等级下的两张查词卡，**并排拼在一起**（用任意图片工具左右拼接，中间留 40px 白缝）：

- 左边标注 `A2`：中文义为主 + 简明英文。
- 右边标注 `C1`：自然的英语解释。

选词建议：选一个中等难度、义项多的词（`giggle`、`grunt`、`sprawl`、`peculiar` 这类），两边差异要肉眼可见。

**可选加分**：在拼图上方放一小条「个人资料 → 英语等级」的设置行截图，说明这个差异是从哪来的。

### 3. `modules.png` —— 自定义模块

**内容**：设置 → 卡片设计，画面里要同时有：

- 自定义模块编辑器，**提示词输入框里有真实内容**——写一个具体的、看得懂的提示词，比如「把这个长句拆成主句和从句，标出每个从句的作用，用中文说明」。别留占位符文字。
- 模块列表，能看到内置模块和自定义模块混排、可拖动排序、有开关。

**要点**：这张图要传达的是「这个软件我能改」。提示词框里的文字是重点，确保它清晰可读。

### 4. `citations.png` —— 可点击的原文引用

**内容**：左右拼接（中间留 40px 白缝），左边是 AI 侧栏，右边是点击之后的正文：

- **左边**：一轮已完成的对话，问的是「把这一章我可能不认识的词和短语列出来」。回答是一份清单，**每一条后面都带着一个角标**（`S1` `S2` `S3`……），回答末尾还有一排来源芯片。清单要足够长，让人看到滚动条——「一次引用三十几处」这件事本身就是说服力。
- **右边**：点了其中一个角标之后的正文页，**被引用的那一句正处在高亮闪烁状态**。左边被点的那一条和右边高亮的那一句，内容要对得上，读者会去核对。

**要点**：这张图讲的是「联动」，所以两边的对应关系必须一眼可见。可以在两边之间画一个箭头，或者把左边被点的那一条和右边高亮的那一句用同一个颜色框出来。**闪烁高亮是转瞬即逝的，可能要多截几次才能抓到**——如果实在抓不到，用录屏截帧。

**可选的第二张 `citations-plot.png`**：问一个剧情问题，回答里带依据和角标，并且能看到「已按你的阅读进度回答（前 X%）」那条阅读思考保护的提示。这一张给推广用，不进 README。

### 5. `wordforms.png` —— 词形标记

**内容**：两部分拼在一起（上下拼接，中间留 40px 白缝）：

- **上半**：设置 → 标记，「标记单词时」的下拉展开，三个选项都可见，选中的是「同单词各词形」；下面展开的词形管理器里有几行真实数据（`run` → `ran, running, runs, run's`），其中一行正在转圈生成。
- **下半**：正文截图，同一页里 `ran`、`running`、`runs` 三个不同词形**都带着标记**。这一半是重点——它证明设置真的生效了。

**要点**：这张图的说服力全在下半部分。上半只讲「有这个选项」，下半才讲「它真的能标到不同词形」。正文里最好能同时看到三种以上词形。

### 6. `markers.png` —— 标记样式与撞色警告（备用）

**内容**：设置 → 标记的样式区，画面里要有：

- 手动 / 自动两套样式的切换，和六个维度的控件（颜色、透明度、高亮 / 下划线 / 加粗、字体）。
- **实时预览那一行示例文字**，里面同时出现朗读洗色、生词 / 学习中 / 已掌握三档下划线、手动标记和自动标记。
- **触发一次撞色警告**：故意把颜色调到接近某个系统标记，让「这个颜色和下面的标记太接近」那行提示出现在画面里。

**要点**：警告文案是这张图的主角。它传达的不是「功能多」，而是「这软件替你想过」。

### 7. `library.png` —— 书库（备用，README 当前没引用）

**内容**：书架网格视图，封面丰满，左侧合集文件夹展开有内容。给推广图用。

### 8. `reading.png` —— 纯净阅读（备用）

**内容**：不带任何弹窗和侧栏的纯阅读画面，暖色纸底，衬线正文。用来反驳「AI 阅读器都很吵」这个印象，也适合做推广图的背景。

## 交付

```
assets/screenshots/
  hero.png
  level.png
  modules.png
  citations.png
  wordforms.png
  markers.png            # 可选
  citations-plot.png     # 可选，推广用
  library.png            # 可选
  reading.png            # 可选
  en/                    # 英文界面的同名文件，给 README.en.md 用
```

`README.md` 里的图片路径已经写好了，图放进去就能显示。英文版如果拍了 `en/`，需要改 `README.en.md` 里的四处路径。

拍完自查：

- [ ] 侧栏显示的是 "Lantern"，不是 "Quill"
- [ ] 画面里没有任何真实 API Key、邮箱、账号
- [ ] 书籍封面用的是公版书
- [ ] 文字在 100% 缩放下清晰可读，没有模糊或缩放伪影
- [ ] 每张 PNG 小于 500 KB

---

## 可直接交给另一个 AI 的提示词

下面这段整段复制给助手即可。

````text
帮我给 Lantern 这个 macOS 桌面应用拍一批产品截图，用于 GitHub README 和社交平台推广。

背景：Lantern 是一款英语原著阅读器，本地优先，接自己的 AI 服务。核心卖点三个——
(1) AI 的解释难度会跟着用户的英语水平（CEFR A1–C2）走；
(2) 查词卡上的模块可以由用户自己写提示词来造；
(3) AI 的每条回答都带可点击的原文引用，点一下就跳回书里对应的那一句。
截图要服务这三个卖点。

项目在 ~/vibecoding/Lantern，用 `npm run tauri dev` 启动。完整的拍摄规范和镜头清单在
docs/guide/screenshots.md，请先读那个文件，然后按它执行。

关键约束：
- 界面截图必须来自真实运行的应用。不要用图像生成模型画界面。
- 窗口 1440×960，Retina 拍摄（输出 2880×1920），PNG，暖色纸感主题，不要深色主题。
- 书库里用公版书（Project Gutenberg 的 Pride and Prejudice、Frankenstein、Moby-Dick 等），
  不要出现在版书的封面。
- 画面里不能出现任何真实 API Key、邮箱或账号名。
- 每张压到 500 KB 以内，存进 assets/screenshots/，文件名严格按清单来。

需要的图（详细要求见 docs/guide/screenshots.md 的「镜头清单」）：
1. hero.png —— 阅读器全景：正文 + 浮在正文上的查词卡 + 展开的 AI 对话侧栏，三者同框
2. level.png —— 同一个词在 A2 和 C1 两个等级下的查词卡并排对比
3. modules.png —— 自定义模块编辑器，提示词框里有真实可读的内容
4. citations.png —— 左：AI 给出的一份长生词清单，每条都带 [S1] 这样的角标；
   右：点击角标之后跳到的正文，被引用那一句正在高亮闪烁。左右要能对得上。
5. wordforms.png —— 上：词形管理器（三档选择 + 已生成的词形）；下：正文里
   run / ran / running 三个词形同时被标记的实拍。下半是重点。
6. markers.png（可选）—— 标记样式设置，画面里要触发一次「颜色太接近」的撞色警告
7. citations-plot.png（可选）—— 剧情问答，回答带依据角标 + 「已按你的阅读进度回答」提示
8. library.png（可选）—— 书架网格 + 合集文件夹
9. reading.png（可选）—— 无任何弹窗的纯净阅读画面

拍完请对照文档末尾的自查清单核对一遍，然后告诉我每张图的实际尺寸和文件大小。
````
